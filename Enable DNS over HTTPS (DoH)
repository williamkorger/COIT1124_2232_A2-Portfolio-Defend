# defend.ps1

# Function to test DNS block status
function TestDNSBlock {
    # Place your code here to retrieve and compare DNS server and IP addresses
    # Capture screenshots as needed
}

# Function to enable DNS over HTTPS
function EnableDoH {
    # Place your code here to modify registry settings and reboot
    # Capture screenshots as needed
}

# Main script
$argument = $args[0]

if ($argument -eq "DoH-test") {
    TestDNSBlock
}
elseif ($argument -eq "DoH-enable") {
    EnableDoH

else {
    Write-Host "Unknown argument: $argument"
}
